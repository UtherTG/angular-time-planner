describe('timePlannerCell Directive', () => {
  beforeEach(angular.mock.module('angularTimePlanner'));

  describe('test test test', () => {
    it('should work', () => {
      expect(2).toEqual(2);
    });
  });
});
